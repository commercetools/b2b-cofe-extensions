import { Store as CommercetoolsStore } from '@commercetools/platform-sdk';
import { Store } from '@Types/store/store';
export declare const mapCommercetoolsStoreToStore: (store: CommercetoolsStore, locale: string, config: Record<string, string>) => Store;
